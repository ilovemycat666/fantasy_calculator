name: Get ESPN Data

on: push
  # schedule:
  #   - cron: '18 1 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo Content
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.5.x'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Scrape ESPN
        run: python backend\env\helpers\espn_getter.py

      - name: Commit Files
        run: |
          git config --local user.email "edwinreik@gmail.com"
          git config --local user.name "ilovemycat666"
          git add -A
          git commit -m "Adds ESPN Data" -a 

      - name: Push Changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: "oYpiGzj*@*GnkRk_N9t@"
          branch: main
